# STM32U5 Secure Password Key

Repository contents:

- `board_spec.json` and generated `STM32U5_Custom_Board.svj` artifacts
- `firmware/` host-testable firmware core
- `docs/` security model, protocol, architecture, and test plan
