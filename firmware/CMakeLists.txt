cmake_minimum_required(VERSION 3.13)
project(stm32u5_password_key_firmware C)

set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS OFF)
option(FIRMWARE_PRODUCTION "Enable production hardening mode" ON)

# STM32U5 specific HAL and drivers
# These would typically be included via a separate STM32CubeMX generated project
# or a dedicated HAL library. For this example, we'll assume they are available.
# You would need to configure STM32CubeMX or similar to generate these.
# For demonstration purposes, we'll add placeholder include directories.
# In a real project, these paths would point to the STM32Cube firmware package.
# Example:
# include_directories(stm32u5_hal_driver stm32u5xx_hal_driver)
# target_link_libraries(firmware_core PRIVATE stm32u5_hal stm32u5xx_hal_driver)

# Add STM32CubeMX generated files if applicable
# add_library(stm32u5_hal STATIC ${STM32_HAL_SOURCES})
# target_include_directories(firmware_core PUBLIC ${STM32_HAL_INCLUDE_DIRS})

add_library(firmware_core
  src/action_engine.c
  src/storage_backend.c
  src/command_codec.c
  src/crypto_engine.c
  src/secure_boot.c
  src/usb_session.c
  src/fido2_authenticator.c
  src/platform_hal.c
  src/rate_limiter.c
  src/security_utils.c
  src/settings_store.c
  src/ui_feedback.c
  src/crypto_stub.c
  src/state_machine.c
  src/password_policy.c
  src/password_generator.c
  src/browser_protocol.c
  src/common_passwords.c
  src/vault.c
  # STM32U5 specific drivers and HAL
  # These would be generated by STM32CubeMX or manually added
  # Example:
  # src/stm32u5xx_hal_msp.c
  # src/stm32u5xx_hal_uart.c
  # src/stm32u5xx_hal_gpio.c
  # src/stm32u5xx_hal_flash.c
  # src/stm32u5xx_hal_i2c.c
  # src/stm32u5xx_hal_spi.c
  # src/stm32u5xx_hal_rcc.c
  # src/stm32u5xx_hal_pwr.c
  # src/stm32u5xx_hal_cortex.c
  # src/system_stm32u5xx.c
)

target_include_directories(firmware_core
  PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/include
    # Add STM32CubeMX generated include directories if applicable
    # Example:
    # ${CMAKE_CURRENT_SOURCE_DIR}/Core/Inc
    # ${CMAKE_CURRENT_SOURCE_DIR}/Drivers/STM32U5xx_HAL_Driver/Inc
)

if(FIRMWARE_PRODUCTION)
  target_compile_definitions(firmware_core PUBLIC FIRMWARE_PRODUCTION=1)
else()
  target_compile_definitions(firmware_core PUBLIC FIRMWARE_PRODUCTION=0)
endif()

# Add STM32U5 specific defines and compiler flags
target_compile_definitions(firmware_core PRIVATE
    "USE_HAL_DRIVER"
    "STM32U5"
    # Add other STM32U5 specific defines as needed
)

# Link against STM32 HAL libraries
# These would typically be generated by STM32CubeMX
# target_link_libraries(firmware_core PRIVATE stm32u5_hal)

add_executable(firmware_tests
  tests/test_main.c
)

target_link_libraries(firmware_tests
  PRIVATE firmware_core
)

enable_testing()
add_test(NAME firmware_tests COMMAND firmware_tests)
