# RP2040 Secure Password Key

Repository contents:

- `board_spec.json` and generated `RP2040_Custom_Board.svj` artifacts
- `firmware/` host-testable firmware core
- `docs/` security model, protocol, architecture, and test plan
